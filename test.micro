PROGRAM expr
BEGIN

	INT a,b,c,d,e;
	FLOAT x;
	STRING newline := "\n";

	FUNCTION VOID main()
	BEGIN
		a := 1;
		b := 2;

          READ(c, d);

          e := a * c + b * 1;
	  x := 1 + 1;
          
	  -- WRITE(c, newline, d, newline, e, newline);

	END	
END
